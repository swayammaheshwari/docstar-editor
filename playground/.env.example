# doc-rtc (Hocuspocus) WS server the playground connects to
VITE_DOCRTC_WS_URL=ws://localhost:1234

# Must match doc-rtc's WORKSPACE_AUTH_TOKEN env var
VITE_WORKSPACE_TOKEN=dev-token

# Any workspace id — doc-rtc scopes storage as `${workspaceId}:${pageId}`
VITE_WORKSPACE_ID=playground
